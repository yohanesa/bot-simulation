# Streaming Server Admin UI — Access Guide

This README explains how to run the StreamingServer service and open its lightweight admin dashboard at /admin/ui.

# TL;DR

docker compose up --build streaming-server

Open http://localhost:8082/admin/ui

Trigger sessions via UserSimulator or Orchestrator to see live updates.